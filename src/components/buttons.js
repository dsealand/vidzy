import React from "react";

import style from "styled-components/native";

/*
 * text.js
 *
 * Should probably make document containing generic buttons to be used in other
 * files so as to not make a bunch of different buttons in every file
 */
