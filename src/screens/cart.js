import React from "react";

import { View, Text, TouchableOpacity } from "react-native";

const Cart = ({ navigation }) => {
    return (
        <View>
            <Text>Cart</Text>
        </View>
    );
};

export default Cart;
